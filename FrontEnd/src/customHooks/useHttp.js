import React, { useCallback, useState } from 'react';

const useHttp = (requestFun, sendingData = false) => {
  const [Loading, setLoading] = useState(false)
  
  const apiRequest = useCallback(async () => {
    
  })
  return (
    <div>
      
    </div>
  );
};

export default useHttp;